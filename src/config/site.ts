// src/config/site.ts

export const siteConfig = {
  megaMenu: {
    // We can define different counts for each screen size
    itemCount: {
      sm: 6,   // For small screens
      md: 9,   // For medium screens
      lg: 12,  // For large screens
      xl: 15,  // For extra-large screens
      '2xl': 18, // For 2XL screens
    },
    filterCount: {
      sm: 5,
      md: 7,
      lg: 8,
      xl: 10,
      '2xl': 12,
       }
  },
};